# LED DJ Controller - React Frontend

DJ-пульт для управления 12 светодиодами Arduino в стиле минимализм с темной темой.

## 🚀 Запуск

```bash
# Установка зависимостей (уже выполнено)
npm install

# Запуск dev сервера
npm run dev
```

Приложение откроется на `http://localhost:3000`

## 📋 Требования

- Python API сервер должен работать на `http://localhost:5001`
- Запустите `python3 led_controller_api.py` в корневой директории проекта

## 🎛️ Возможности

- **12 больших кнопок** в стиле DJ-пульта
- **Темная тема** с неоновыми акцентами
- **Минималистичный дизайн**
- **Реактивное обновление** состояния светодиодов
- **Анимации** свечения при включении
- **Адаптивная верстка** для разных экранов

## 🎨 Дизайн

- Темный градиентный фон
- Неоновый зеленый (#00ff88) для активных состояний
- Большие кнопки с эффектом глубины
- Анимированные индикаторы LED
- Эффекты наведения и нажатия

## 🔧 Технологии

- React 18
- TypeScript
- Vite
- CSS3 с анимациями
- Fetch API для взаимодействия с бэкендом

## 📁 Структура

```
src/
├── api/
│   └── ledApi.ts           # API клиент
├── components/
│   ├── DJController.tsx    # Основной контроллер
│   ├── DJController.css
│   ├── LEDButton.tsx       # Кнопка светодиода
│   └── LEDButton.css
├── App.tsx
├── App.css
├── main.tsx
└── index.css
```

## 🎯 Использование

1. Запустите Python API сервер
2. Запустите React приложение
3. Откройте браузер на `http://localhost:3000`
4. Нажимайте на кнопки для управления светодиодами
5. Кнопки подсвечиваются при включении LED

## 🌟 Функции кнопок

- **Клик** - переключить состояние светодиода
- **Подсветка** - показывает текущее состояние (вкл/выкл)
- **Анимация** - эффект свечения при активном состоянии
- **Номер** - ID светодиода (1-12)
- **Pin** - номер пина Arduino (2-13)
